package com.vishnuthangaraj.wishlist.Enums;

public enum Gender {

    /*
        ENUM NAME : Gender
        DESCRIPTION : This enum represents a set of predefined gender values, providing a
                      standardized and inclusive way to capture gender information within an
                      application. This enum is commonly used to promote consistency and avoid
                      ambiguity when dealing with gender-related data.
        ENUMERATED VALUE : - `MALE`: Represents the male gender.
                           - `FEMALE`: Represents the female gender.
                           - `OTHER`: Represents a non-binary or other gender identity.
    */

    MALE,
    FEMALE,
    OTHER

}
