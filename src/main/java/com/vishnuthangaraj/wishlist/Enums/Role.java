package com.vishnuthangaraj.wishlist.Enums;

public enum Role {

    /*
        ENUM NAME : Role
        DESCRIPTION : This enum defines a set of pre-defined roles within an application, assigning
                      distinct levels of access or permissions to users. This enum is commonly
                      used to manage and differentiate user roles, facilitating role-based
                      access control and security measures.
        ENUMERATED VALUE : - `USER`: Represents a standard user role with basic access and functionality.
                           - `ADMIN`: Represents an administrative role with elevated access and control privileges.
    */

    USER,
    ADMIN

}
